{
  "name": "qustar-monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "bootstrap": "npm run build && npm install --workspaces",
    "deploy": "npm run deploy --workspaces",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "@types/node": "20.8.2",
    "@vitest/coverage-v8": "^1.6.0",
    "gts": "^5.2.0",
    "prettier": "^3.3.2",
    "prettier-plugin-organize-imports": "^3.2.3",
    "rimraf": "^5.0.7",
    "tsx": "^4.15.6",
    "typescript": "~5.1.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "semver": "^7.6.3",
    "axios": "^1.7.2"
  }
}
